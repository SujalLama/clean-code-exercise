/*
Comments: Remove Noise & Add Clarifying Comments

Goal:
Remove redundant comments.
Add a clarifying comment if needed (e.g., for edge cases).
*/

// bad code

// This is a function
function sum(a, b) {
  // add a and b
  return a + b; // returning result
}

// write good code here

// function to get sum of two numbers
function sum(a, b) {
  return a + b;
}
